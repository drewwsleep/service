# ОПИСАНИЕ ФАЙЛОВ ПРОЕКТА

## Структура файлов

```
course/
├── src/                           # Исходные файлы
│   ├── index.html                 # ✅ Главная страница (247 строк)
│   ├── login.html                 # ✅ Страница входа
│   ├── reg.html                   # ✅ Страница регистрации
│   ├── css/
│   │   ├── style.css              # 📄 Скомпилированный CSS из LESS
│   │   ├── style.min.css          # 📄 Минифицированный CSS
│   │   └── style.css.map          # 📄 Source map для отладки
│   ├── js/
│   │   ├── script.js              # ✅ Основной скрипт (420+ строк)
│   │   └── script.min.js          # 📄 Минифицированный JS
│   └── less/
│       ├── style.less             # ✅ Основные стили (653 строк)
│       ├── var.less               # ✅ Переменные цветов (25 строк)
│       ├── login.less             # ✅ Стили входа (104 строк)
│       ├── reg.less               # ✅ Стили регистрации (104 строк)
│       └── adaptive.less          # ✅ Адаптивные стили
│
├── Gruntfile.js                   # 📄 Конфигурация Grunt
├── package.json                   # 📄 Зависимости Node.js
├── package-lock.json              # 📄 Lock файл зависимостей
│
├── README.md                       # 📖 Основная документация
├── REQUIREMENTS.md                # 📖 Описание требований
├── USAGE.md                       # 📖 Примеры использования
├── QUICKSTART.md                  # 📖 Быстрый старт
└── FILES.md                       # 📖 Этот файл
```

## Детальное описание файлов

### HTML Файлы

#### 📄 src/index.html (247 строк)
**Назначение:** Главная страница приложения

**Структура:**
- **Header** с навигацией и кнопками входа/регистрации
- **Main** раздел с 4 основными секциями:
  1. **#services** — каталог из 6 услуг
  2. **#parts** — каталог из 6 запчастей
  3. **#booking** — форма бронирования и список записей
  4. **#admin** — админ-панель с управлением и статистикой
- **Footer** с информацией о проекте

**Главные элементы:**
```html
<section id="services">           <!-- Услуги -->
<section id="parts">              <!-- Запчасти -->
<section id="booking">            <!-- Запись -->
<aside id="admin">                <!-- Администрирование -->
```

**Использование:** Откройте в браузере для просмотра основного интерфейса

---

#### 📄 src/login.html
**Назначение:** Страница входа в систему

**Содержит:**
- Единый дизайн с главной страницей
- Форма входа (email + пароль)
- Ссылка на страницу регистрации

**Элементы:**
```html
<form id="loginForm">
  <input type="email" name="email">
  <input type="password" name="password">
</form>
```

---

#### 📄 src/reg.html
**Назначение:** Страница регистрации новых пользователей

**Содержит:**
- Единый дизайн с остальным сайтом
- Форма регистрации (5 полей)
- Ссылка на страницу входа

**Поля:**
- Имя
- Email
- Телефон
- Пароль
- Повтор пароля

---

### CSS/LESS Файлы

#### 📄 src/less/var.less (25 строк) ⭐ ГЛАВНЫЙ ФАЙЛ ДЛЯ ДИЗАЙНА
**Назначение:** Централизованные переменные для всего сайта

**Содержит:**
```less
// Основные цвета
--bg-main: #f5f5f5;              // Фон страницы
--bg-dark: #1e1e1e;              // Тёмный фон
--bg-light: #fff;                // Светлый фон
--text-main: #333;               // Основной текст
--text-light: #666;              // Светлый текст

// Функциональные цвета
--primary-color: #2196F3;        // Синий (основной)
--primary-hover: #1976D2;        // Синий при наведении
--success-color: #4CAF50;        // Зелёный (успех)
--success-hover: #388E3C;        // Зелёный при наведении
--danger-color: #f44336;         // Красный (опасность)
--warning-color: #ff9800;        // Оранжевый
--info-color: #00bcd4;           // Голубой (информация)

// Эффекты
--shadow: 0 2px 5px rgba(...);
--shadow-dark: 0 5px 15px rgba(...);
--radius: 5px;                   // Скругление углов

// Размеры
--spacing: 15px;                 // Основной отступ
```

**Как использовать:**
```less
// В других LESS файлах используйте переменные:
button {
  background: var(--primary-color);
  box-shadow: var(--shadow);
  border-radius: var(--radius);
}
```

**Изменение дизайна:**
1. Отредактируйте цвет в var.less
2. Запустите `grunt build`
3. Все элементы изменятся автоматически

---

#### 📄 src/less/style.less (653 строк) ⭐ ОСНОВНОЙ СТИЛЬ ФАЙЛ
**Назначение:** Основные стили всех элементов страницы

**Структура (вложенные селекторы):**
```less
/* HEADER */
header {
  .header-top { ... }
  nav { ... }
  .auth-buttons { ... }
}

/* СЕКЦИИ */
section { ... }

/* УСЛУГИ */
.services-grid { ... }
.service-card { ... }

/* ЗАПЧАСТИ */
.parts-grid { ... }
.part-card { ... }

/* ФОРМА БРОНИРОВАНИЯ */
.booking-container { ... }
.booking-form { ... }
.booking-list { ... }

/* АДМИНИСТРИРОВАНИЕ */
.admin-stats { ... }
.admin-controls { ... }
.admin-records { ... }

/* FOOTER */
footer { ... }
```

**Ключевые классы:**
- `.service-card` — карточка услуги
- `.part-card` — карточка запчасти
- `.booking-form` — форма бронирования
- `.admin-record-item` — запись в админ-панели

---

#### 📄 src/less/login.less (104 строк)
**Назначение:** Стили для страницы входа

**Содержит:**
- Стили для `.auth-page` (центрирование)
- Стили для `.auth-container` (карточка входа)
- Стили для формы входа
- Адаптивные стили для мобильных

**Особенности:**
- Использует переменные из `var.less`
- Цветовая схема: синий `--primary-color`
- Анимации при наведении

---

#### 📄 src/less/reg.less (104 строк)
**Назначение:** Стили для страницы регистрации

**Аналогично login.less, но:**
- Цветовая схема: зелёный `--success-color`
- Больше полей в форме

---

#### 📄 src/less/adaptive.less
**Назначение:** Адаптивные стили для мобильных устройств

**Медиа-запросы:**
```less
@media (max-width: 768px) {
  // Планшеты и мобильные
}

@media (max-width: 480px) {
  // Только мобильные
}
```

---

#### 📄 src/css/style.css (Генерируется!)
**Назначение:** Скомпилированный CSS из LESS файлов

**⚠️ НЕ РЕДАКТИРУЙТЕ ВРУЧНУЮ!**

**Процесс генерации:**
1. Grunt читает LESS файлы
2. Компилирует их в CSS
3. Сохраняет результат в style.css

**Пересобрать:**
```bash
grunt build
```

---

### JavaScript Файлы

#### 📄 src/js/script.js (420+ строк) ⭐ ОСНОВНАЯ ЛОГИКА
**Назначение:** Вся логика приложения

**Разделы (в порядке следования):**

1. **ДАННЫЕ** (строки 1-50)
   ```javascript
   const services = {
     "Замена масла и фильтров": {
       price: 2000,
       parts: [...]
     },
     ...
   }
   ```
   - 6 услуг с ценами и запчастями
   - 6 запчастей с ценами

2. **КОНСТАНТЫ** (строки 52-56)
   ```javascript
   const STORAGE_KEY = "service_records";
   ```

3. **DOM ЭЛЕМЕНТЫ** (строки 58-75)
   - Ссылки на элементы HTML

4. **ФУНКЦИИ ЗАГРУЗКИ** (строки 77-84)
   - `loadRecords()` — загрузить из localStorage
   - `saveRecords()` — сохранить в localStorage

5. **ФУНКЦИИ ОТОБРАЖЕНИЯ** (строки 86-156)
   - `displayUpcomingBookings()` — показать ближайшие
   - `displayAdminRecords()` — показать все в админ-панели
   - `updateStatistics()` — обновить статистику

6. **ФУНКЦИИ УПРАВЛЕНИЯ** (строки 158-195)
   - `deleteRecord()` — удалить запись
   - `exportToCSV()` — экспорт в CSV
   - `formatDate()` — форматировать дату

7. **ОБРАБОТЧИКИ СОБЫТИЙ** (строки 197-232)
   - Обработка формы бронирования
   - Обработка кнопок выбора услуги
   - Обработка кнопок администрирования

8. **ИНИЦИАЛИЗАЦИЯ** (строки 234-240)
   - Загрузка при открытии страницы

**Главные функции:**

```javascript
// Создать запись
const record = {
  clientName: "...",
  service: "...",
  date: "...",
  time: "...",
  total: 4000
};
records.push(record);
saveRecords(records);

// Отобразить записи
displayUpcomingBookings();
displayAdminRecords();

// Обновить статистику
updateStatistics();

// Экспортировать CSV
exportToCSV();
```

---

### Конфигурационные файлы

#### 📄 Gruntfile.js
**Назначение:** Конфигурация автоматизации сборки

**Содержит:**
- Конфигурация задач LESS
- Конфигурация минификации
- Конфигурация для копирования файлов

**Основные задачи:**
```bash
grunt build           # Собрать проект
grunt serve          # Запустить сервер с автообновлением
grunt less           # Только компилировать LESS
grunt clean          # Очистить dist папку
```

---

#### 📄 package.json
**Назначение:** Зависимости проекта

**Содержит:**
```json
{
  "name": "course",
  "version": "1.0.0",
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-contrib-less": "^3.0.0",
    "grunt-contrib-uglify": "^5.2.2",
    "grunt-contrib-cssmin": "^5.0.0",
    "grunt-contrib-watch": "^1.1.0",
    ...
  }
}
```

**Установка:**
```bash
npm install
```

---

### Документационные файлы

#### 📖 README.md
**Содержит:**
- Описание проекта
- Структуру проекта
- Требования и установку
- Использование
- Лицензию

---

#### 📖 REQUIREMENTS.md
**Содержит:**
- Все требования курсовой работы
- Проверку выполнения каждого требования
- Описание файлов
- Функции JavaScript
- Результаты

---

#### 📖 USAGE.md
**Содержит:**
- 7 сценариев использования
- Структуру данных
- Примеры для консоли браузера
- Контрольный список
- Возможные улучшения

---

#### 📖 QUICKSTART.md
**Содержит:**
- Шаг за шагом установку
- Команды для сборки
- Работу с LESS и JavaScript
- Распространённые проблемы
- Тестирование на мобильных

---

#### 📖 FILES.md (этот файл)
**Содержит:**
- Описание структуры проекта
- Детальное описание каждого файла
- Как использовать каждый файл
- Что редактировать для изменения

---

## Какой файл редактировать для...?

### 🎨 Изменить цвета и дизайн
**Отредактируйте:** `src/less/var.less`
```less
--primary-color: #2196F3;  // Измените цвет
```
Потом: `grunt build`

---

### 📝 Добавить новую услугу
**Отредактируйте:** `src/js/script.js` (строки 1-50)
```javascript
const services = {
  "Новая услуга": {
    price: 3000,
    parts: [
      { name: "Запчасть 1", price: 1500 }
    ]
  }
}
```

И добавьте в HTML `src/index.html` (раздел #services):
```html
<article class="service-card">
  <h3>Новая услуга</h3>
  <p class="price">3000 ₽</p>
  ...
</article>
```

---

### 🔧 Добавить новое поле в форму
**Отредактируйте:** `src/index.html` (раздел #booking)
```html
<div class="form-group">
  <label for="newField">Новое поле:</label>
  <input type="text" id="newField" name="newField">
</div>
```

И обновите JavaScript в `src/js/script.js`:
```javascript
const newField = document.getElementById("newField").value;
const record = {
  ...
  newField: newField
};
```

---

### 💾 Изменить хранилище с localStorage на БД
**Отредактируйте:** `src/js/script.js`

Замените функции:
```javascript
function loadRecords() {
  // Вместо: return JSON.parse(localStorage.getItem(STORAGE_KEY))
  // Используйте: fetch('api/records')
}

function saveRecords(records) {
  // Вместо: localStorage.setItem(...)
  // Используйте: fetch('api/records', {method: 'POST', body: ...})
}
```

---

### 📱 Добавить мобильное меню
**Отредактируйте:** `src/less/adaptive.less`

Добавьте:
```less
@media (max-width: 768px) {
  header nav {
    // Скройте меню
    display: none;
    
    &.active {
      // Покажите при клике
      display: block;
    }
  }
}
```

И в `src/js/script.js`:
```javascript
document.getElementById("menuButton").addEventListener("click", function() {
  document.querySelector("header nav").classList.toggle("active");
});
```

---

## Порядок загрузки файлов в браузере

```
1. index.html (или login.html / reg.html)
   ↓
2. src/css/style.min.css (стили)
   ↓
3. src/js/script.js (скрипты)
   ↓
4. Страница полностью загружена и готова
   ↓
5. DOMContentLoaded событие запускает инициализацию
```

---

## Размеры файлов

| Файл | Размер | Строк |
|------|--------|-------|
| src/index.html | ~8 KB | 247 |
| src/login.html | ~3 KB | 35 |
| src/reg.html | ~3 KB | 40 |
| src/js/script.js | ~15 KB | 420+ |
| src/less/style.less | ~25 KB | 653 |
| src/less/var.less | ~1 KB | 25 |
| src/css/style.css (скомп.) | ~30 KB | 800+ |
| **ИТОГО** | **~85 KB** | **2,200+** |

---

## Типичный цикл разработки

```bash
# 1. Отредактируйте LESS или HTML
# nano src/less/var.less
# или
# nano src/index.html

# 2. Пересоберите
grunt build

# 3. Обновите браузер
# F5 или Ctrl+Shift+R

# 4. Проверьте результат
# Откройте DevTools (F12)
# Проверьте консоль на ошибки
```

---

## Версионирование

**Текущая версия:** 1.0.0

**История:**
- v1.0.0 — Первый релиз (все требования выполнены)

---

**Последнее обновление:** 2025-01-06

**Для вопросов смотрите README.md или QUICKSTART.md**
